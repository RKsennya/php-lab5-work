# Работа с глобальными переменными $_POST и $_REQUEST в PHP

## Цель
Познакомиться с глобальными переменными `$_POST` и `$_REQUEST` в PHP. 
Научиться валидировать пользовательские данные, работать с различными типами элементов формы, а также анализировать различия между `$_REQUEST` и `$_POST`.

## Условие

### Задание 1: Работа с глобальной переменной $_POST
1. Изучите следующий код и проанализируйте его работу:
   ```html
   <div class="form">
       <form action="<?php echo $_SERVER['PHP_SELF'] ?>" method="POST">
           <!-- Форма с полями для ввода -->
       </form>
       <div id="result">
           <p>Ваше имя: <b><?php echo $_POST["name"] ?? ''; ?></b></p>
           <!-- Вывод данных из формы -->
       </div>
   </div>

## Объясните, что такое глобальные переменные $_POST и $_SERVER["PHP_SELF"].
<p><strong>$_POST</strong> - это суперглобальная переменная PHP, которая используется для сбора данных, отправленных методом HTTP POST из HTML-формы. Данные передаются невидимо для пользователя, они не отображаются в URL.</p>
    <p><strong>$_SERVER["PHP_SELF"]</strong> - это переменная, которая возвращает имя текущего выполняемого скрипта относительно корня документа. Используется для отправки данных формы на тот же скрипт, который её отображает.</p>


### Задание 2: Получение данных с различных контроллеров
1. Создайте форму с 5 различными элементами управления:
Минимум 1 input с типом number
Минимум 1 select
Минимум 1 radio или checkbox
Определите тему формы на своё усмотрение (например, регистрация на мероприятие).

Обработайте отправленные данные и выведите их на экран.


### Задание 3: Создание, обработка и валидация форм
1. Создайте  форму, показанную на рисунке. Реализуйте валидацию для следующих полей:

Поле "name":
Минимальная длина: 3 символа.
Максимальная длина: 20 символов.
Запрет на использование цифр.
Поле "mail":
Проверка, что адрес электронной почты соответствует стандартам.
Поле "comment":
Должно быть заполнено.
Дополнительные критерии по вашему выбору.
Галочка "Do you agree with data processing?":
Пользователь обязан её отметить перед отправкой формы.
Если пользователь ввёл данные корректно, выведите сообщение ниже формы.

Объясните, чем отличаются глобальные переменные $_REQUEST и $_POST.
 $_POST - используется только для данных, отправленных методом POST. Это значит, что если форма отправляется с помощью метода POST, данные будут доступны в $_POST.
Безопаснее для передачи конфиденциальной информации, так как данные не отображаются в URL.
$_REQUEST содержит данные из нескольких источников: как из $_POST, так и из $_GET (данные, переданные через URL). Это значит, что $_REQUEST объединяет данные, отправленные разными методами.
Менее безопасен, так как может включать данные из URL, которые могут быть видны пользователю.

### Задание 4: Создание теста
1. Создайте  форму теста с:

Тремя вопросами.
Элементами input с типом radio и checkbox.
Полем для ввода имени пользователя.
Реализуйте проверку заполнения всех полей формы и выберите ответы пользователя.

Выведите результаты теста на экран.
